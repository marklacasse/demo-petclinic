# DISCLAIMER: This workflow file has been auto-generated and committed to the repo by the GitHub App from Contrast Security.
# Manual edits to this file could cause the integration to produce unexpected behavior or break the integration.
# Version: 1.0.0
# Last updated: 2026-04-22T21:12:51.940290610Z
name: Contrast CI

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *' # <-- 6:00 UTC (2:00 AM EST / 11:00 PM PST)
  pull_request:
    types:
      - closed
  push:
    branches:
      - master

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  # --- Authenticating if using an LLM from AWS Bedrock ---
  # Option A: Configure AWS Credentials using IAM (Recommended for production)
  # You must uncomment the `aws_region` and `use_aws_creds_action` lines below in the action inputs
  # You must configure an organization or repo variable with a valid AWS_REGION
  #aws-login:
    #name: Configure AWS Credentials
    #runs-on: ubuntu-latest
    #steps:
      #- name: Configure AWS Credentials
        #id: aws-credentials
        #uses: aws-actions/configure-aws-credentials@v5
        #with:
          #aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          #aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          #aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          #aws-region: ${{ vars.AWS_REGION }}
          #role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
    # Option B: Use Bedrock API Keys (Simpler but less secure)
    # If using API keys, omit the "Configure AWS Credentials" step above
    # You must configure an organization or repo variable with a valid AWS_REGION
    # You must uncomment the aws_region lines below in the action inputs
    # and set AWS_BEARER_TOKEN_BEDROCK organization secret with bedrock API key.
  call-contrast-orchestrator:
    if: ${{ github.actor != 'dependabot[bot]' }}
    name: Contrast CI
    uses: Contrast-Security-OSS/contrast-ci/.github/workflows/contrast-orchestrator.yml@v1
    with:
      # -------------------------------------------------------
      # CONTRAST PLATFORM CONFIG (set by Contrast GitHub App at onboard)
      # -------------------------------------------------------
      api_host:                   ${{ vars.CONTRAST_CI_API_HOST }}
      contrast_org_id:            ${{ vars.CONTRAST_CI_ORG_ID }}

      # -------------------------------------------------------
      # REPO CONTEXT (auto-populated, do not edit)
      # -------------------------------------------------------
      base_branch: ${{ github.event.repository.default_branch }}
      repo_url:    ${{ github.server_url }}
      repo_name:   ${{ github.repository }}

      # -------------------------------------------------------
      # RUNNER (optional override)
      # -------------------------------------------------------
      github_runner: ${{ vars.CONTRAST_CI_GITHUB_RUNNER || 'ubuntu-latest' }}

      # -------------------------------------------------------
      # EA PRODUCT DISABLE FLAGS
      # Set to 'true' to disable a product for this repo
      # -------------------------------------------------------
      disable_contrast_code:  ${{ vars.CONTRAST_CI_DISABLE_CONTRAST_CODE || 'false' }}
      disable_contrast_sca:  ${{ vars.CONTRAST_CI_DISABLE_CONTRAST_SCA || 'false' }}
      disable_smartfix:  ${{ vars.CONTRAST_CI_DISABLE_CONTRAST_SMARTFIX || 'false' }}

      # -------------------------------------------------------
      # SmartFix Config (manual optional overrides)
      # -------------------------------------------------------
      #contrast_app_id: ${{ vars.CONTRAST_APP_ID }}
      #build_command: ''
      #max_open_prs: ${{ vars.CONTRAST_CI_MAX_PRS || '5' }}
      #formatting_command: ''
      #enable_full_telemetry: ${{ vars.CONTRAST_CI_ENABLE_FULL_TELEMETRY || 'false' }}
      #debug_mode: ${{ vars.CONTRAST_CI_DEBUG_MODE || 'true' }}
      # Coding Agent Options: 'SMARTFIX', 'GITHUB_COPILOT', 'CLAUDE_CODE'
      #coding_agent: ${{ vars.CONTRAST_CI_CODING_AGENT || 'SMARTFIX' }}

      # -------------------------------------------------------
      # Contrast Code Config (manual optional overrides)
      # Uncomment below to override defaults.
      # -------------------------------------------------------
      #run_triage: ${{ vars.CONTRAST_CI_RUN_TRIAGE || 'False' }}
      #triage_duration: ${{ vars.CONTRAST_CI_TRIAGE_DURATION || '10m' }}

      # -------------------------------------------------------
      # LLM CONFIG
      # Defaults to Contrast-hosted LLM.
      # Uncomment below to use your own LLM provider.
      # -------------------------------------------------------
      use_contrast_llm: ${{ vars.CONTRAST_CI_USE_CONTRAST_LLM || 'true' }}
      # set `use_aws_creds_action` to true and `aws_region` when using the aws-actions/configure-aws-credentials action
      #use_aws_creds_action: ${{ vars.CONTRAST_CI_USE_AWS_CREDS_ACTION || 'false' }}
      #aws_region:       ${{ vars.AWS_REGION }}
      #agent_model:      ${{ vars.AGENT_MODEL }} # e.g. 'bedrock/us.anthropic.claude-sonnet-4-5-20250929-v1:0'
      #azure_api_base:   ${{ vars.CONTRAST_CI_AZURE_API_BASE || '' }}
      #azure_api_version: ${{ vars.CONTRAST_CI_AZURE_API_VERSION || '' }}

    secrets:
      # -------------------------------------------------------
      # REQUIRED (Contrast values set by Contrast GitHub App at onboard)
      # -------------------------------------------------------
      CONTRAST_API_KEY:           ${{ secrets.CONTRAST_CI_API_KEY }}
      CONTRAST_AUTHORIZATION:     ${{ secrets.CONTRAST_CI_AUTHORIZATION }}
      GH_TOKEN:                   ${{ secrets.GITHUB_TOKEN }}

      # -------------------------------------------------------
      # OPTIONAL — BYOLLM
      # If using your own LLM provider, set the relevant API keys in your
      # GitHub organization secrets and uncomment the corresponding lines below.
      # -------------------------------------------------------
      #ANTHROPIC_API_KEY:        ${{ secrets.ANTHROPIC_API_KEY }}
      #GEMINI_API_KEY:           ${{ secrets.GEMINI_API_KEY }}
      #AZURE_API_KEY:            ${{ secrets.AZURE_API_KEY }}
      #AWS_BEARER_TOKEN_BEDROCK: ${{ secrets.AWS_BEARER_TOKEN_BEDROCK }}
      #AWS_ROLE_TO_ASSUME:       ${{ secrets.AWS_ROLE_TO_ASSUME }}
      #AWS_ACCESS_KEY_ID:        ${{ secrets.AWS_ACCESS_KEY_ID }}
      #AWS_SECRET_ACCESS_KEY:    ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #AWS_SESSION_TOKEN:        ${{ secrets.AWS_SESSION_TOKEN }}
